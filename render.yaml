databases:
  - name: teachedos-db
    databaseName: teachedos
    user: teachedos_user
    plan: free
    region: frankfurt
    ipAllowList: []

services:
  - type: web
    name: teachedos-api
    runtime: node
    region: frankfurt
    plan: free
    branch: main
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: ALLOWED_ORIGINS
        value: https://munister-v.github.io
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: teachedos-db
          property: connectionString
